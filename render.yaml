services:
  - type: web
    name: smartlist-api
    runtime: node
    plan: free
    buildCommand: "npm install && npx prisma generate && npx prisma migrate deploy"
    startCommand: "npm start"
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 3000
